function getItemCategory(inputMessage) {
	const itemCategory = [];
	itemCategory.push("Hello There");
	return itemCategory;
}

function getRequestType(inputMessage) {
	const requestType = [];
	return requestType;
}

function getBudget(inputMessage) {
	let budget = 0;
	return budget;
}

function getRecomendation(inputMessage) {
	return recomendation;
}

export const recomendation = (inputMessage) => {
	return inputMessage;
};
